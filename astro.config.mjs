// @ts-check
import { defineConfig } from 'astro/config';

// ─────────────────────────────────────────────────────────────
// EDIT THIS: replace YOUR-USERNAME with your GitHub username.
//
// If your repo is named exactly "YOUR-USERNAME.github.io", you're
// done — leave `base` commented out.
//
// If your repo has any other name (e.g. "personal-site"), also
// uncomment the `base` line and put the repo name there.
// ─────────────────────────────────────────────────────────────

export default defineConfig({
  site: 'https://camp2679.github.io',
  // base: '/personal-site',
});
